const searchWrapper=document.querySelector(".search-input"),inputBox=searchWrapper.querySelector("input"),suggBox=searchWrapper.querySelector(".autocom-box"),icon=document.getElementById("search"),imagesContainer=document.querySelector(".imagesContainer");function select(element){let selectData=element.textContent;inputBox.value=selectData,icon.onclick=()=>{processText(selectData)},searchWrapper.classList.remove("active")}function showSuggestions(list){let listData;list.length?listData=list.join(""):(userValue=inputBox.value,listData=`<li>${userValue}</li>`),suggBox.innerHTML=listData}function processText(userData){console.log(userData+"is the Raw Input");let Check1=element=>element.toLowerCase().trim()===userData.toLowerCase().trim();const indexCheck1=suggestions.findIndex(Check1);indexCheck1>-1?(console.log("Sentence Found in the Box"),genImgCard(userData,indexCheck1)):(console.log("Loose Processing the data"),looseProcess(userData))}function genImgCard(userData,indexCheck1){const newCard=document.createElement("div");newCard.setAttribute("class","newCard"),imagesContainer.appendChild(newCard);const newImg=document.createElement("img"),Label=document.createElement("p");Label.textContent=userData,newImg.setAttribute("class","imgOutput"),newImg.src=`../sentences/s${indexCheck1}.png`,newCard.appendChild(newImg),newCard.appendChild(Label)}function looseProcess(signText){const Text=signText.toUpperCase().trim();console.log(Text);for(let i in Text)console.log(Text[i]),getImg(Text[i])}function getImg(letter){const newImg=document.createElement("img");newImg.setAttribute("class","looseImages"),newImg.src=`../Alpha/${letter}.png`,imagesContainer.appendChild(newImg)}function clearScreen(){inputBox.value="";const node=document.querySelector(".imagesContainer");for(;node.hasChildNodes();)node.removeChild(node.lastChild)}inputBox.onkeyup=e=>{let userData=e.target.value,emptyArray=[];if(userData){icon.onclick=()=>{processText(userData)},emptyArray=suggestions.filter(data=>data.toLocaleLowerCase().startsWith(userData.toLocaleLowerCase())),emptyArray=emptyArray.map(data=>`<li>${data}</li>`),searchWrapper.classList.add("active"),showSuggestions(emptyArray);let allList=suggBox.querySelectorAll("li");for(let i=0;i<allList.length;i++)allList[i].setAttribute("onclick","select(this)")}else searchWrapper.classList.remove("active")};